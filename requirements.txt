# ── Core scientific stack ─────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
statsmodels==0.14.2

# ── Dask (distributed / out-of-core ETL) ─────────────────────────────────────
# [complete] pulls bokeh, distributed, s3fs, etc. — needed for the FAERS ETL
# which processes multi-GB quarterly zips without loading all into RAM.
dask[complete]==2024.5.2

# ── PyTorch (CPU build — swap cu121 wheel for GPU cluster) ───────────────────
# Pinned to a version that has a matching torch_geometric wheel.
torch==2.3.1+cpu --extra-index-url https://download.pytorch.org/whl/cpu
torchvision==0.18.1+cpu --extra-index-url https://download.pytorch.org/whl/cpu

# ── PyTorch Geometric ─────────────────────────────────────────────────────────
# torch_geometric itself is pure-Python; the C++ extensions (pyg_lib,
# torch_scatter, torch_sparse) need matching torch+CUDA versions.
torch_geometric==2.5.3
torch_scatter==2.1.2 --find-links https://data.pyg.org/whl/torch-2.3.1+cpu.html
torch_sparse==0.6.18  --find-links https://data.pyg.org/whl/torch-2.3.1+cpu.html

# ── Causal inference ─────────────────────────────────────────────────────────
# econml: Microsoft's DoublyRobustLearner, DML, etc.
# causalml: Uber's CATE estimators (used as a cross-check on Day 19)
econml==0.15.1
causalml==0.15.1

# ── API layer ────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4

# ── Database drivers ─────────────────────────────────────────────────────────
# psycopg2-binary: no libpq build dep needed (binary wheel)
psycopg2-binary==2.9.9
# neo4j driver is here but the compose service is commented out until Day 10
neo4j==5.21.0
sqlalchemy==2.0.30

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
httpx==0.27.0        # async test client for FastAPI

# ── Notebook / exploration ────────────────────────────────────────────────────
jupyterlab==4.2.2
matplotlib==3.9.0
seaborn==0.13.2
plotly==5.22.0
